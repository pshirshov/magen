mapping:
  - id: "select-all"
    binding: 
      - "ctrl-k ctrl-a"
    vscode:
      action: "editor.action.selectAll"
    zed:
      action: "editor::SelectAll"
      context:
        - Editor
        - Terminal
    idea:
      action: "$SelectAll"    
